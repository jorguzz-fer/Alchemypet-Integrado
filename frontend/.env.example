# URL base da API REST (backend FastAPI) do Painel Convenio.
# Todas as chamadas do frontend usam esta variavel como base.
# Deve ser exposta ao browser (prefixo NEXT_PUBLIC_).
NEXT_PUBLIC_API_URL=https://api-alchemypet.tudomudou.com.br
